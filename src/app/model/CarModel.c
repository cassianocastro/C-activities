#include "../../includes/model/CarModel.h"

Car createCar(void)
{
    return (Car)
    {
        .color   = "",
        .mark    = "",
        .model   = "",
        .placa   = "",
        .release = 0
    };
}